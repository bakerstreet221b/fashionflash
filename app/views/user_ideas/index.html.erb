<h1><%= @ideas.first.name %></h1>

<table>
  <tr>
    <th>Description</th>
    <th>Picture</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ideas.each do |idea| %>
  <tr>
    <td><%= link_to idea.description, idea %></td>
    <td><%= image_tag idea.picture_url, :width => '10%' if idea.picture.present?%></td>

    <td>
      <% if can? :update, idea %>
        <%= link_to 'Edit', edit_idea_path(idea) %>
      <% end %>
    </td>
    <td>
      <% if can? :destroy, idea %>
        <%= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' } %>
      <% end %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'back', ideas_path %>
